-- Script compatible con pgAdmin Query Tool
-- Ejecuta esto conectado a la base "postgres" u otra base administrativa.
CREATE DATABASE app_pollos_el_dorado;

-- Luego conectate manualmente a la base "app_pollos_el_dorado"
-- y ejecuta en otra consulta:
-- CREATE EXTENSION IF NOT EXISTS "pgcrypto";
